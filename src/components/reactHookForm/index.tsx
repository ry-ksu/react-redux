import React from 'react';

export const ReactHookForm = () => {
  return (
    <>
      {/* <input
        {...register('lastName', {
          required: 'This is required',
          pattern: {
            value: /^[A-Za-z]+$/i,
            message: 'Name will not include numbers',
          },
          minLength: {
            value: 2,
            message: 'Min length is 2',
          },
        })}
      />
      <p>{errors.lastName && String(errors.lastName.message)}</p> */}
    </>
  );
};
